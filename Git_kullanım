Commit hakkında bilgi edinme = "git log"

Hangi dosyada hangi işlem yapıldığını yazdıran git komutu = " git blame dosya adı"

Hangi branch(dalda) olduğumuzu gösteren komut = "git branch"

Farklı bir branch oluşturma = "git branch isim"   (isim=oluşturmak istediğiniz dal adı)

Oluşturulan branch'a geçmek için = "git checkout isim"

Branch birleştirmek için "git merge master isim" = (Master=Diğer Branch'ın ismi)

Oluşturulan Branch'ı silmek için = "git branch -d isim" 

Oluşturulan Branch'ı Git deposuna yüklemek için = " git push origin isim"


YENİ BRANCH OLUŞTURUP DOSYA EKLEME

1.GitHub'la bağlantı kurulan klasörün içerisine giriniz 

"cd git/"


2.Klasöre içerisine girdikten sonra yeni branch oluşturmak için;

" git branch deneme"


3.Oluşturulan Branch'a geçinizi.

" git checkout deneme " 


4.Yeni dosya oluşturunuz ve içeresine kullanmak istediğiniz metni giriniz.

" touch dosya " 


5.Oluşturulan dosyayı Git Deponuza ekleyiniz.

" git add . "


6.Oluşturulan dosyaya açıklama giriniz.

" git commit -m "güncel" "


7.Sonraki işlemde yeni oluşturduğunuz Branch'tan diğer sabit olan Branch'a geçmek için;


"git checkout master"     


8.Bulunduğunuz Branch'ı kontrol ediniz.


" git branch"


9.Oluşturulan deneme Branch'ı ile master Branch'ını birleştiriniz.


" git merge master deneme "


10.Son işlem dosyayı GitHub deponuza aktarınız


"git push origin master" 
   















